enum RiskState { riskInit acceptable notAcceptable }

enum CommunicationState { communicationInit listening standby sent }
enum ConditionsState { conditionsInit changed same}

enum MissingPersonState { missingPersonInit found missing }
enum SearchState {searchInit on off }

var risk: RiskState

var comms: CommunicationState
var conditions: ConditionsState

var missingPerson: MissingPersonState
var search: SearchState
