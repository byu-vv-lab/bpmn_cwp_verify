enum RiskState { riskInit ground air both }

enum CommunicationState { communicationInit received failed }
enum ConditionsState { conditionsInit changed same}

enum MissingPersonState { missingPersonInit found missing }
enum SearchState {searchInit on off }

var risk: RiskState = riskInit

var communication: CommunicationState = communicationInit
var conditions: ConditionsState = conditionsInit

var missingPerson: MissingPersonState = missingPersonInit
var search: SearchState = searchInit