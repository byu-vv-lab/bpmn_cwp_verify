enum all { INIT other}
enum TermsType {agreed failed pending noRetry}
enum OwnerType {buyerName sellerName}

const paymentAmount: byte
const belowPaymentAmount: byte
const noRetryPayment: byte
const pendingPayment: byte

var terms:TermsType
var backpackOwner: OwnerType
var paymentOwner: OwnerType
var paymentOffered: byte
